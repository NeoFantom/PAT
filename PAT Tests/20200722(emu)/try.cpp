#include <algorithm>
#include <cstdio>

int f() {
    int x = 2;
    return x = 3;
}

int main(int argc, char const *argv[]) {
    // int a[] = {5, 3, 7, 2, 9};
    // std::sort(a, a + 5);
    // std::swap(a[0], a[1]);
    // for (int i = 0; i < 5; ++i) printf("%d ", a[i]);
    double x = 1.8 * 1.01 * 1.01 * 1.01;
    printf("%.2lf", 3.5234);
    return 0;
}
